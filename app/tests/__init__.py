"""Pytest suite for the Bazaar merchant core.

Converted from the imperative scripts in scripts/test_*.py, which remain
as one-line shims so the commands in the README and the devlog keep
working. The check logic is unchanged; only the harness around it moved.

    uv run pytest -q
"""
